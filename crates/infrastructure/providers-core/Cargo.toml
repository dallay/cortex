[package]
name = "providers-core"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
# Serialization — needed for JSON validation and sanitization in provider adapters
serde = { workspace = true }
serde_json = { workspace = true }