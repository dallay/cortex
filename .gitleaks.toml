[top]
quiet = true
verbose = false
no-git = false

[rules]
allowlist = []

[[rules]]
description = "Generic API Key"
regex = '''(?i)(api[_-]?key|apikey|api[_-]?token|apitoken)['"]?\s*[=:]\s*['"]?[a-zA-Z0-9_-]{20,}'''
hits = ["api_key", "apiKey", "api_token", "APITOKEN"]

[[rules]]
description = "AWS Access Key"
regex = '''AKIA[0-9A-Z]{16}'''
hits = ["AKIA"]

[[rules]]
description = "AWS Secret Key"
regex = '''(?i)aws[_-]?secret[_-]?access[_-]?key\s*[=:]\s*['"]?[a-zA-Z0-9/+=]{40}'''
hits = ["aws_secret"]

[[rules]]
description = "Generic Secret"
regex = '''(?i)(secret|password|passwd|pwd|token)['"]?\s*[=:]\s*['"]?[a-zA-Z0-9_-]{8,}'''
hits = ["secret", "password", "token"]

[[rules]]
description = "OpenAI/GCP/Hugging Face API Key pattern"
regex = '''sk-[a-zA-Z0-9]{20,}|AIza[a-zA-Z0-9_-]{35}|hf_[a-zA-Z0-9]{34,}'''
hits = ["sk-", "AIza", "hf_"]

[allowlist]
paths = [
  '''node_modules/.*''',
  '''target/.*''',
  '''\.git/.*''',
  '''\.github/workflows/.*''',
  '''reports/.*''',
  '''.*\.lock''',
  '''.*\.md''',
]