change: dynamic-provider-registry
current_phase: sdd-apply
completed:
  - init
  - sdd-spec
  - sdd-design
  - sdd-tasks
  - sdd-apply
next: Phase 5 unit tests (5.1-5.17) + Phase 6 integration tests (6.1-6.6)
updated: "2026-05-31T00:00:00Z"
pr1_status: |
  PR1 (Phases 1-4) COMPLETE. All foundation work done:
  - RegistryError enum added to ports.rs
  - ProviderRegistryPort extended with replace_all/upsert/remove
  - DecryptedCredentials enum created in rook-core
  - ConnectionConfig.base_url added
  - FallbackRouter.providers changed to Arc<RwLock<Vec<...>>>
  - FallbackRouter::new_empty() constructor added
  - parking_lot RwLock used (not tokio::sync::RwLock — different types for different use cases)
  - round_robin_index uses AsyncRwLock (tokio) for .write().await
  - refresh_registry() implemented in ManageConnections (full implementation, not stub)
  - decrypt_credentials() implemented
  - RegistryUpdateFailed error added to ManageConnectionsError
  - Mock registries in tests updated with replace_all/upsert/remove
  - All base_url fields added to test fixtures
  - clippy warnings fixed (map_clone)
  - Tests: 56 passed in rook-usecases, 5 passed in rook-core
pr2_status: |
  PR2 Phase 4 (DI wiring) COMPLETE — 2026-05-31:
  Phase 4 tasks 4.1-4.8 all DONE:
  - ProviderBuildError enum added (OllamaRequiresBaseUrl, ConstructionFailed)
  - build_provider_from_connection() implemented with all 5 providers + OAuth support
  - build_provider() TOML function removed from di.rs
  - providers: Vec<ProviderConfig> removed from RookConfig
  - ProviderConfig struct and expansion loop removed from config.rs
  - RookContainer::build now uses FallbackRouter::new_empty + refresh_registry()
  - build_provider_from_connection exported as pub(crate)
  - docs updated: [[providers]] sections removed from configuration.md and providers.md
  - workspace tests: 217 passed, clippy clean
notes: |
  Key implementation decisions made during PR2 Phase 4:
  - OAuth access_token is used as api_key where the provider supports it (OpenAI, Anthropic, etc.)
  - Ollama requires base_url (returns OllamaRequiresBaseUrl if missing)
  - refresh_registry made pub (not pub(crate)) to allow calling from di.rs across crate boundary
  - On startup refresh failure, Rook logs warning and starts with empty registry (graceful degradation)
  - ProviderBuildError UnsupportedKind variant removed (ProviderKind is exhaustive, all 5 variants handled)
  - unused provider imports removed from di.rs (use fully-qualified paths in build_provider_from_connection)
