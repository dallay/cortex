name: 👋 Greetings

on:
  pull_request:
    types: [opened, reopened]
  issues:
    types: [opened, reopened]

permissions:
  issues: write
  pull-requests: write

jobs:
  greeting:
    name: 👋 Welcome Contributor
    uses: dallay/common-actions/.github/workflows/greetings.yml@v2.2.0
    secrets: inherit